import { Text, View } from 'react-native';

const K3s = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>K3s</Text>
    </View>
  );
};

export default K3s;
